{{> admin-menu active="settings"}}

<section class="section">
  <h1 class="title">Edit User Information</h1>
  <form action="/adminsettings/editAdminUser" method="POST">
    <label class="label">Name</label>
    <div class="field is-horizontal">
      <div class="field-body">
        <div class="field">
          <input class="input" type="text" placeholder="{{user.firstName}}" name="firstName">
        </div>
        <div class="field">
          <input class="input" type="text" placeholder="{{user.lastName}}" name="lastName">
        </div>
      </div>
    </div>
    <div class="field is-grouped">
      <button class="button is-link">Submit</button>
      <a href="/usersettings/deleteuser/{{user._id}}" class="button">
    </span>
    </a>
    </div>
  </form>
</section>